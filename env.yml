name: rag-agent
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - fastapi
  - uvicorn
  - chromadb>=0.5.5
  - numpy<2
  - pandas
  - pip
  - redis-py
  # 可選：不要在 conda 裝 tesseract/poppler，避免衝突 
  # pip install langchain<0.2.0
  - pip:
      - langchain
      - langgraph
      - sentence-transformers
      - rank-bm25
      - unstructured
      - pymupdf
      - sse-starlette
      - pydantic>=2
      - ragas
      - trulens-eval
